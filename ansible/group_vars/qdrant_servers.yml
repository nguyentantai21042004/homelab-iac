---
# Qdrant servers group variables

# Qdrant version
qdrant_version: "latest"

# Stack path
qdrant_stack_path: "/mnt/qdrant_data/qdrant-stack"

# Ports
qdrant_http_port: 6333
qdrant_grpc_port: 6334

# API Key (optional)
# For student projects, no auth is fine (internal network only)
qdrant_api_key: ""
