# API Gateway (Traefik) configuration
traefik_stack_path: "/opt/traefik"

# Let's Encrypt email
traefik_acme_email: "nguyentantai.dev@gmail.com"

# Traefik Dashboard credentials
traefik_dashboard_user: "tantai"
traefik_dashboard_password: "{{ vault_traefik_dashboard_password }}"

# Backend services IP addresses (reference service_ips from group_vars/all/common.yml)
storage_backend_ip: "{{ service_ips.storage }}"

# LocalStack IP
localstack_ip: "{{ service_ips.localstack }}"

# Woodpecker CI IP
woodpecker_ip: "{{ service_ips.cicd }}"

# K3s Cluster VIP
k3s_vip: "{{ service_ips.k3s_vip }}"
